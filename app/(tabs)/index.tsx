import { Image, StyleSheet, Platform, Text, View } from 'react-native';

export default function HomeScreen() {
  return (
    <View>
      <Text style={{fontSize:40, color:'white',padding:'5%'}}>Home</Text>
    </View>
  );
}